import 'package:flutter/material.dart';
import 'flavor.dart';

class NeuroLoopApp extends StatelessWidget {
  final Flavor flavor;

  const NeuroLoopApp({super.key, required this.flavor});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Container(),
    );
  }

}
